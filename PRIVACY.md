# 隐私与安全

`pi-tui` 作为 Pi 扩展在用户本机运行，与 Pi 进程拥有相同权限。

## 本机读取

- 当前工作目录和 Git 状态；
- Pi 会话中的模型、Token、Cache、Context 和费用字段；
- Pi 模型注册表解析后的供应商地址与凭据；
- Pi 全局目录中的 `pi-tui.json`、插件缓存和当前会话文件。

## 网络访问

只有供应商被可靠识别，或者用户在 `pi-tui.json` 中显式配置查询协议时，插件才查询余额或订阅额度。未知地址不会被探测；当前推理凭据不会自动发送到不同域名。GitHub Enterprise、控制面凭据和额外查询 Key 只有在用户显式配置后才使用。

## 本地保存

- 用量缓存位于 Pi 全局缓存目录的 `pi-tui/usage/` 下；
- 缓存保存不可逆账号指纹和归一化用量快照，不保存 API Key 原文；
- 单轮耗时和回复尾遥测保存为 Pi 自定义会话条目，不进入模型上下文；
- 插件不会默认记录完整提示词、回复正文或供应商原始响应。

## 用户控制

卸载插件不会删除历史 Pi 会话。用量缓存可以单独删除并自动重建。报告问题时不要上传 API Key、OAuth Token、完整配置、完整会话文件或未经检查的终端日志。
